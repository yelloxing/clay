/**
 * 包含布局
 *
 */
clay.packLayout = function () {

    var scope={

    };

    /**
     *
     * @param {any} initPack 可以被配置方法解析的数据
     */
    var pack = function (initPack) {

    };

    return pack;
};
